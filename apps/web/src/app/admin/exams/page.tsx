'use client';
import { useEffect,useState } from 'react'; import { api } from '@/lib/api-client'; import { Badge, SectionTitle } from '@/components/ui';
export default function AdminExamsPage(){const [rows,setRows]=useState<{id:string;title:string;status:string;detectedLevel:string;examType?:{code:string}}[]>([]);useEffect(()=>{api.listExams().then(x=>setRows(x as typeof rows)).catch(()=>undefined)},[]);return <section><SectionTitle eyebrow="Content sources" title="Exams" description="Imported exam files and their analysis status."/><div className="grid gap-3">{rows.length===0?<div className="rounded-2xl bg-white p-6 text-sm text-slate-500">No exam sources available yet.</div>:rows.map(r=><div key={r.id} className="flex flex-col gap-3 rounded-2xl bg-white p-5 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div><p className="font-bold">{r.title}</p><p className="mt-1 text-xs text-slate-500">{r.examType?.code ?? 'General'} · {r.detectedLevel}</p></div><Badge tone={r.status==='PUBLISHED'?'green':'amber'}>{r.status}</Badge></div>)}</div></section>}
