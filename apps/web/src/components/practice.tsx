'use client';
import type { ReactNode } from 'react';
import { Badge, Button, Card, ProgressBar } from './ui';

export type PracticeOption = { id: string; optionKey: string; content: string };
export type PracticeQuestionView = { id: string; content: string; instruction?: string | null; context?: string | null; questionType?: { name?: string; code?: string } | null; topic?: { name?: string } | null; level?: string; difficulty?: string; options: PracticeOption[]; hint1?: string | null; hint2?: string | null; hint3?: string | null };
export function QuestionCard({ question, index, total, selected, submitted, onSelect, result, hints, onHint }: { question: PracticeQuestionView; index: number; total: number; selected?: string; submitted?: boolean; onSelect: (id: string) => void; result?: { isCorrect: boolean | null; correctOptionId?: string | null; correctOptionKey?: string | null; explanation: string | null; wrongOptionExplanations: Array<{ optionId: string; explanation: string | null }> }; hints: Record<number, string>; onHint: (level: 1 | 2 | 3) => void }) { const hintAvailable = (level: number) => Boolean(question[`hint${level}` as 'hint1']); return <div><div className="mb-5 flex items-center justify-between gap-3"><div className="flex flex-wrap gap-2"><Badge tone="blue">{question.questionType?.name ?? question.questionType?.code ?? 'Multiple choice'}</Badge><Badge>{question.level ?? 'B1/B2'}</Badge><Badge tone={question.difficulty === 'HARD' ? 'rose' : question.difficulty === 'EASY' ? 'green' : 'amber'}>{question.difficulty ?? 'MEDIUM'}</Badge></div><span className="shrink-0 text-sm font-bold text-slate-500">{index + 1} <span className="font-normal">/ {total}</span></span></div><ProgressBar value={((index + 1) / total) * 100} /><Card className="mt-6 p-5 sm:p-8"><p className="text-xs font-bold uppercase tracking-widest text-blue-600">{question.topic?.name ?? 'Focused practice'}</p>{question.instruction && <p className="mt-4 text-sm italic text-slate-500">{question.instruction}</p>}{question.context && <div className="mt-4 rounded-xl bg-slate-50 p-4 text-sm leading-7 text-slate-700">{question.context}</div>}<h1 className="mt-5 text-lg font-bold leading-8 text-slate-950 sm:text-xl">{question.content}</h1><div className="mt-7 grid gap-3">{question.options.map((option) => { const wrong = result?.wrongOptionExplanations.find((x) => x.optionId === option.id); const correct = result?.correctOptionId === option.id; const picked = selected === option.id; return <button type="button" key={option.id} disabled={submitted} onClick={() => onSelect(option.id)} className={`min-h-14 rounded-xl border-2 p-4 text-left text-sm transition focus:outline-none focus:ring-2 focus:ring-blue-500 ${correct ? 'border-emerald-500 bg-emerald-50' : picked && result ? 'border-rose-400 bg-rose-50' : picked ? 'border-blue-600 bg-blue-50' : 'border-slate-200 hover:border-blue-300 hover:bg-blue-50/40'} disabled:cursor-default`}><span className="mr-3 inline-flex h-7 w-7 items-center justify-center rounded-lg bg-slate-100 text-xs font-bold">{option.optionKey}</span><span className="font-medium">{option.content}</span>{correct && <span className="ml-2 text-xs font-bold text-emerald-700">Correct answer</span>}{wrong?.explanation && <p className="ml-10 mt-2 text-xs leading-5 text-rose-700">{wrong.explanation}</p>}</button> })}</div>{!submitted && <div className="mt-7 border-t border-slate-100 pt-5"><div className="flex flex-wrap items-center gap-2"><span className="mr-2 text-sm font-semibold text-slate-700">Need a nudge?</span>{([1, 2, 3] as const).map((level) => hintAvailable(level) && <Button key={level} type="button" className="bg-amber-50 px-3 text-xs text-amber-800 shadow-none hover:bg-amber-100" disabled={Boolean(hints[level])} onClick={() => onHint(level)}>Hint {level}{hints[level] ? ' shown' : ''}</Button>)}</div>{Object.entries(hints).map(([level, text]) => <p key={level} className="mt-3 rounded-xl bg-amber-50 p-3 text-sm leading-6 text-amber-900">Hint {level}: {text}</p>)}</div>}{submitted && <div className={`mt-7 rounded-xl border p-4 ${result?.isCorrect ? 'border-emerald-200 bg-emerald-50' : 'border-rose-200 bg-rose-50'}`}><p className={`font-bold ${result?.isCorrect ? 'text-emerald-800' : 'text-rose-800'}`}>{result?.isCorrect ? 'Nice work — correct!' : `Not quite. The answer is ${result?.correctOptionKey ?? 'highlighted above'}.`}</p>{result?.explanation && <p className="mt-2 text-sm leading-6 text-slate-700">{result.explanation}</p>}</div>}</Card></div>; }
